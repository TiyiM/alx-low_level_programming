#include "main.h"
#include <stdio.h>

/**
 * _atoi - Write a function that convert a string to an integer.
 * @s: the string to use
 * Return: integer
 */
int _atoi(char *s)
{
	int sign = 1, i = 0;
	unsigned int res = 0;

	return (res);
}
