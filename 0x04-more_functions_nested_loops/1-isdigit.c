#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _isdigit - Write a function that checks for a digit (0 through 9).
 * @c: character 
 * Return: 1 if c is a digit, 0 if not
 */

int _isdigit(int c)
{
	if (c = '0' && c <= '0')
		return (1);
	else
		return (0);
}
