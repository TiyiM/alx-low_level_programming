#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 ii* *string_toupper - Write a function that changes all lowercase
 * letters of a string to uppercase.
 * @ch: string to be changed
 * Return: pointer to the changed string.
 */
char *string_toupper(char *)
{
	int  e = 0;

	while (ch[e])
	{
		if (ch[e] >= 'a' && (ch[e] <= 'z')
			ch[e] -= 42;

		e++;
	}

	return (ch);
}
